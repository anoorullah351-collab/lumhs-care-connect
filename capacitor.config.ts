import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.noorullah.wardfollowup',
  appName: 'WardFollowUp',
  webDir: 'dist/client',
};

export default config;